var user ={name:"Arief", age:11 }
console.log("Arief");
console.log(user.name);


//
// console.log(user.email);

// so there is an error .
// when I assign an variable in the form 
// let or Const in the TS it shows the error 
// It only accept var.

// "let" user ={name:"Arief", age:11 }  // error
// "const" user ={name:"Arief", age:11 }  // error